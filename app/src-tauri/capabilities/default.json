{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capability for the Daybook window. Phase 0 needs only core: the four probe commands are registered through invoke_handler, which does not require an ACL entry.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
