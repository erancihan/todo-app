# Rust
/target
**/target

# Node
node_modules/
.npm/

# Vite / build output
app/dist/
spikes/*/dist/
spikes/*/pkg/

# Tauri generated
app/src-tauri/gen/
app/src-tauri/target/

# Relay runtime data (one directory — backup is a snapshot of it)
daybook-data/
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# Phase 0 spike artifacts (regenerate with `npm run build:wasm` / run-linux.sh)
spikes/wasm-opfs/pkg/

spikes/wasm-opfs/crate/target/

# Mobile signing secrets — never commit
app/src-tauri/gen/android/keystore.properties
*.jks
*.keystore

# Generated wasm bindings for the browser build (npm run build:wasm)
app/src/core/wasm/

# Headless desktop-run artifacts (scripts/run-linux-desktop.sh)
target/desktop-run/
